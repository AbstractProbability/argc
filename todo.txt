1. Parse arguments provided on command line. Provide api.
2. Short options, long options, options expected to be followed by some param (e.g. in `gcc -o main main.c`, `-o` is expected to be followed by some param, although it should be something like `gcc -o=main main.c`)
3. utils: regex-handling maybe? String utilities, etc.
4. Mapping between short and long options, automatic short options generator, normaliser (short and long options for the same things must be normalised into the same longOption)


Immediate:
1. Tests for init_arg_template: default options, what if I pass NULL (i.e. no defaults)
2. Ergonomics: user cannot use the library without much malloc boilerplate, how to reduce this
    - E.g. user has to make an HEAP array of args and pass it, instead can they just pass it normally inline
3. Tests for parse_args
    - Currently parameterOptions are not working properly, the parameter options are not being recognised.
